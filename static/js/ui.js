/*
ui.js

Handles user interactions:

reads slider values (diffusion D, timestep dt, k, potential type)

pause, reset, start buttons

dropdown menu for potentials

passes updated values to simulation.js

This keeps UI logic separate from physics */
