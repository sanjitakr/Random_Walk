[project]
name = "randomwalk"
version = "0.1.0"
description = "Random Walk & Diffusion Simulator"
authors = [{name="Sanjita Kumar"}]
dependencies = ["numpy", "h5py"]


[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
